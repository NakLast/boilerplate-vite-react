import type { Config } from "prettier";

const config: Config = {
  arrowParens: "avoid",
  useTabs: false,
  tabWidth: 2,
  printWidth: 100,
  bracketSameLine: false,
  trailingComma: "es5",
  endOfLine: "auto",
  singleQuote: false,
  semi: true,
};

export default config;
